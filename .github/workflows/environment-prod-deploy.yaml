name: Environment Prod - Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Terraform Apply - Prod
    runs-on: ubuntu-latest
    environment: prod-rw
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Terraform Apply
        uses: dflook/terraform-apply@v1
        with:
          path: terraform/environment/prod
          label: prod
