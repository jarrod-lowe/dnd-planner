// Rules Engine v2 — public surface (M0 spike, dataflow subset).
export { evaluateSheet } from './sheet';
export { statToModifier } from './functions';
export type {
  Facts,
  FactReader,
  CombineMode,
  Contribution,
  SheetCtx,
  RuleModule
} from './types';
