name: 'Hold Person - Planned Item Illegal While Concentrating'
description: >-
  Casting Hold Person, then planning a second cast while concentration is
  still held, marks the second planned item illegal with an already_concentrating
  error. A slot remains, so no_slots does not co-fire.
ruleGroups:
  - dnd-5e-2024/ability-scores
  - dnd-5e-2024/proficiency
  - dnd-5e-2024/action-economy
  - dnd-5e-2024/free-actions
  - dnd-5e-2024/spellcasting
  - dnd-5e-2024/concentration
  - dnd-5e-2024/core-events
  - class-paladin/class-paladin-level1
  - spells/spell-hold-person
initialEffects:
  - id: test-grant-level2-slots
    phase: early
    group:
      - spellcasting-slots-set
    activities:
      - type: numberSet
        target:
          fact: spellcasting.slots.level2.total
        source:
          number: 2
      - type: numberSet
        target:
          fact: spellcasting.slots.level2.remaining
        source:
          number: 2
      - type: advertiseEffect
        self: true
steps:
  - evaluate:
  - addOffer:
      id: prepare-hold-person
  - endTurn: {}
  # Turn 1: cast Hold Person, starting concentration
  - addOffer:
      id: cast-hold-person
      assert:
        facts:
          concentration.remaining: 0
          spellcasting.slots.level2.remaining: 1
  - endTurn:
      assert:
        effects:
          exists:
            - effect-hold-person
  # Turn 2: still concentrating; planning a second cast is illegal (concentration),
  # but a slot still remains so no_slots does not fire.
  - evaluate:
      assert:
        facts:
          concentration.remaining: 0
          spellcasting.slots.level2.remaining: 1
  - addOffer:
      id: cast-hold-person
      assert:
        planErrors:
          - id: cast-hold-person
            index: 0
            errors:
              - rule.spell-hold-person.offer-hold-person.already_concentrating
