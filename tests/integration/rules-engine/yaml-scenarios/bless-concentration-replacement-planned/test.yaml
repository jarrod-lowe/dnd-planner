name: 'Bless - Planned Recast Legal and Replacing While Concentrating'
description: >-
  Casting Bless, then planning a second cast while concentration is still held,
  is legal (SRD 5.2) and replaces the hold: no plan errors, and the keyed
  concentration effect keeps concentration.spent at 1 instead of stacking.
ruleGroups:
  - dnd-5e-2024/prepared-spells
  - dnd-5e-2024/ability-scores
  - dnd-5e-2024/proficiency
  - dnd-5e-2024/action-economy
  - dnd-5e-2024/free-actions
  - dnd-5e-2024/spellcasting
  - dnd-5e-2024/concentration
  - dnd-5e-2024/core-events
  - class-paladin/class-paladin-level1
  - spells/spell-bless
steps:
  - evaluate:
  - addOffer:
      id: set-prepared-spells
      selections:
        prepared:
          - spellId: bless
            level: 1
            nameKey: rule.spell-bless.offer-bless.name
            preparedFact: spell.l1.bless.prepared
            alwaysPreparedFact: spell.l1.bless.alwaysPrepared
  - evaluate:
      assert:
        facts:
          concentration.remaining: 1
        offers:
          exists:
            - cast-bless
  - addOffer:
      id: cast-bless
      assert:
        facts:
          concentration.remaining: 0
          spellcasting.slots.level1.remaining: 1
  - endTurn:
      assert:
        effects:
          exists:
            - effect-bless
  - evaluate:
      assert:
        facts:
          concentration.remaining: 0
          spellcasting.slots.level1.remaining: 1
  # Turn 2: still concentrating; planning a second cast is LEGAL (SRD 5.2 — the
  # recast dismisses the first hold) and a slot remains, so NO errors at all.
  - addOffer:
      id: cast-bless
      assert:
        planErrors:
          - id: cast-bless
            index: 0
            errors: []
        facts:
          concentration.spent: 1
          spellcasting.slots.level1.remaining: 0
