name: 'Divine Smite - Legal With Higher Slot Only'
description: 'A prepared Divine Smite is legal when level 1 slots are exhausted but a higher-level slot is available'
ruleGroups:
  - dnd-5e-2024/action-economy
  - dnd-5e-2024/free-actions
  - dnd-5e-2024/proficiency
  - dnd-5e-2024/ability-scores
  - dnd-5e-2024/core-events
  - dnd-5e-2024/attacks
  - dnd-5e-2024/spellcasting
  - class-paladin/class-paladin-level1
  - spells/spell-divine-smite
initialEffects:
  # Simulate having a level 2 spell slot (e.g., from a higher paladin level)
  - id: test-grant-level2-slot
    phase: early
    group:
      - spellcasting-slots-set
    activities:
      - type: numberSet
        target:
          fact: spellcasting.slots.level2.total
        source:
          number: 1
      - type: numberSet
        target:
          fact: spellcasting.slots.level2.remaining
        source:
          number: 1
      - type: advertiseEffect
        self: true
steps:
  # Prepare first
  - evaluate: {}
  - addOffer:
      id: prepare-divine-smite
      assert:
        facts:
          bonusActions.remaining: 1
          spellcasting.remaining: 1
          spellcasting.slots.level1.remaining: 2
          spellcasting.slots.level2.remaining: 1
          # Highest slot level by total capacity (the granted L2 slot)
          spellcasting.maxSlotLevel: 2
  # Attack first (required for smite)
  - addOffer:
      id: unarmed-strike-use-action
      assert:
        facts:
          attack.last.activation.action: 1
          bonusActions.remaining: 1
          spellcasting.remaining: 1
          spellcasting.slots.level1.remaining: 2
          spellcasting.slots.level2.remaining: 1
        offers:
          legal:
            - cast-divine-smite
  # Smite using L1 slot (first)
  - addOffer:
      id: cast-divine-smite
      selections:
        slotLevel: 1
      assert:
        facts:
          bonusActions.remaining: 0
          spellcasting.remaining: 0
          spellcasting.slots.level1.remaining: 1
          spellcasting.slots.level2.remaining: 1
  - endTurn: {}
  # Turn 2: Attack and smite again using L1 to exhaust it
  - addOffer:
      id: unarmed-strike-use-action
      assert:
        facts:
          attack.last.activation.action: 1
          bonusActions.remaining: 1
          spellcasting.remaining: 1
          spellcasting.slots.level1.remaining: 1
          spellcasting.slots.level2.remaining: 1
  - addOffer:
      id: cast-divine-smite
      selections:
        slotLevel: 1
      assert:
        facts:
          bonusActions.remaining: 0
          spellcasting.remaining: 0
          spellcasting.slots.level1.remaining: 0
          spellcasting.slots.level2.remaining: 1
  - endTurn: {}
  # Turn 3: Attack - smite should be LEGAL (L1 exhausted, L2 available)
  - addOffer:
      id: unarmed-strike-use-action
      assert:
        facts:
          attack.last.activation.action: 1
          bonusActions.remaining: 1
          spellcasting.remaining: 1
          spellcasting.slots.level1.remaining: 0
          spellcasting.slots.level2.remaining: 1
        offers:
          legal:
            - cast-divine-smite
