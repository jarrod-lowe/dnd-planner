# Route53 hosted zone domain name (must exist in the AWS account)
domain = "example.com"

# Host/subdomain prefix for the CDN (e.g., "test" results in test.example.com)
host = "test"

# SNS topic ARN for CloudWatch alarm notifications
sns_alarm_topic_arn = "arn:aws:sns:region:account-id:topic-name"
