# yaml-language-server: $schema=../schema.json
ruleGroups:
  - id: base
    name: rule.dnd-5e-2024.base.name
    description: rule.dnd-5e-2024.base.description
    rules:
      # Movement Actions
      - id: action-move-reset
        phase: early
        after:
          - group: species-constants
        activities:
          - type: numberCopy
            source:
              fact: character.movement.total
            target: character.movement.current
      - id: action-move-walk-offer
        activities:
          - type: offerRule
            legalWhen:
              - condition:
                  fact: character.movement.current
                  operator: greaterThan
                  value: 0
                illegalDiagnostics:
                  - code: rule.dnd-5e-2024.base.action-move-walk-offer.out_of_movement
                    severity: error
            rule:
              id: move-walk
              ui:
                model: move
                section: move
                name: rule.dnd-5e-2024.base.move-walk.name
              vars:
                distance:
                  capture: true
                  default:
                    fact: character.movement.current
                maxDistance:
                  default:
                    fact: character.movement.total
              activities:
                # Consume remaining movement
                - type: numberIncrement
                  target: character.movement.current
                  source:
                    var: distance
                  subtract: true
      - id: action-free-offer
        activities:
          - type: offerRule
            rule:
              id: free-action
              ui:
                model: free
                section: free
                name: rule.dnd-5e-2024.base.free-action.name
              activities: []
