{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://dnd-planner.local/rule-groups",
  "title": "D&D Planner Rule Groups",
  "description": "Schema for D&D Planner rule group YAML files",
  "type": "object",
  "required": ["ruleGroups"],
  "properties": {
    "ruleGroups": {
      "type": "array",
      "description": "Array of rule groups to sync to DynamoDB",
      "items": {
        "$ref": "#/definitions/ruleGroup"
      }
    }
  },
  "definitions": {
    "ruleGroup": {
      "type": "object",
      "required": ["id"],
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for this rule group",
          "examples": ["paladin-level1-divine-sense", "fighter-champion-improved-critical"]
        },
        "name": {
          "type": "string",
          "description": "i18n key for display name",
          "examples": ["ruleGroups.paladin.divineSense.name"]
        },
        "description": {
          "type": "string",
          "description": "i18n key for description",
          "examples": ["ruleGroups.paladin.divineSense.description"]
        },
        "rules": {
          "type": "array",
          "description": "Array of rules in this rule group",
          "items": {
            "$ref": "#/definitions/rule"
          }
        }
      },
      "additionalProperties": false
    },
    "rule": {
      "type": "object",
      "required": ["id", "activities"],
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for this rule"
        },
        "description": {
          "type": "string",
          "description": "i18n key for rule description"
        },
        "ui": {
          "type": "object",
          "description": "UI-specific configuration",
          "additionalProperties": true
        },
        "vars": {
          "type": "object",
          "description": "Rule variables with default values",
          "additionalProperties": {
            "$ref": "#/definitions/varDefinition"
          }
        },
        "selections": {
          "type": "object",
          "description": "User-selected values for vars",
          "additionalProperties": true
        },
        "phase": {
          "$ref": "#/definitions/phase",
          "default": "normal"
        },
        "enabled": {
          "type": "boolean",
          "description": "Whether this rule is enabled",
          "default": true
        },
        "when": {
          "type": "array",
          "description": "Conditions that must be satisfied",
          "items": {
            "$ref": "#/definitions/condition"
          }
        },
        "after": {
          "type": "array",
          "description": "Groups that must settle first",
          "items": {
            "$ref": "#/definitions/groupReference"
          }
        },
        "group": {
          "type": "array",
          "description": "Groups this rule belongs to",
          "items": {
            "type": "string"
          }
        },
        "activities": {
          "type": "array",
          "description": "Actions to perform",
          "items": {
            "$ref": "#/definitions/activity"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    },
    "phase": {
      "type": "string",
      "enum": ["early", "normal", "safeguard"],
      "default": "normal"
    },
    "condition": {
      "type": "object",
      "oneOf": [
        { "$ref": "#/definitions/factExistenceCondition" },
        { "$ref": "#/definitions/factComparisonCondition" },
        { "$ref": "#/definitions/eventCondition" }
      ]
    },
    "factExistenceCondition": {
      "type": "object",
      "required": ["fact"],
      "properties": {
        "fact": {
          "type": "string",
          "description": "Fact path to check"
        }
      },
      "additionalProperties": false
    },
    "factComparisonCondition": {
      "type": "object",
      "required": ["fact", "operator", "value"],
      "properties": {
        "fact": {
          "type": "string",
          "description": "Fact path to compare"
        },
        "operator": {
          "$ref": "#/definitions/comparisonOperator"
        },
        "value": {
          "type": ["number", "string"],
          "description": "Value to compare against"
        }
      },
      "additionalProperties": false
    },
    "comparisonOperator": {
      "type": "string",
      "enum": [
        "equals",
        "notEquals",
        "greaterThan",
        "greaterThanOrEqual",
        "lessThan",
        "lessThanOrEqual"
      ]
    },
    "eventCondition": {
      "type": "object",
      "required": ["event"],
      "properties": {
        "event": {
          "type": "string",
          "description": "Event name to check for"
        }
      },
      "additionalProperties": false
    },
    "groupReference": {
      "type": "object",
      "required": ["group"],
      "properties": {
        "group": {
          "type": "string",
          "description": "Group name"
        }
      },
      "additionalProperties": false
    },
    "varDefinition": {
      "type": "object",
      "required": ["default"],
      "properties": {
        "default": {
          "$ref": "#/definitions/source",
          "description": "Default value for the variable"
        },
        "capture": {
          "type": "boolean",
          "description": "When true, resolve the default value from facts and store as selection when the rule is added to the plan"
        }
      },
      "additionalProperties": false
    },
    "source": {
      "type": "object",
      "description": "A unified value reference - exactly one of number, fact, or var",
      "minProperties": 1,
      "maxProperties": 1,
      "properties": {
        "number": {
          "type": "number",
          "description": "A literal numeric value"
        },
        "fact": {
          "type": "string",
          "description": "Reference to a fact in working state"
        },
        "var": {
          "type": "string",
          "description": "Reference to a rule variable"
        }
      },
      "additionalProperties": false
    },
    "activity": {
      "type": "object",
      "required": ["type"],
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "numberSet",
            "numberIncrement",
            "numberCopy",
            "numberSum",
            "numberFunction",
            "emitEvent",
            "generateRule",
            "offerRule"
          ]
        },
        "target": {
          "type": "string",
          "description": "Target fact path"
        },
        "source": {
          "$ref": "#/definitions/source",
          "description": "Source value reference (for numberSet, numberIncrement, numberCopy)"
        },
        "sources": {
          "type": "array",
          "items": { "$ref": "#/definitions/source" },
          "description": "Array of source value references (for numberSum, numberFunction)"
        },
        "max": {
          "type": "string",
          "description": "Maximum value fact path (for numberIncrement)"
        },
        "subtract": {
          "type": "boolean",
          "description": "For numberIncrement: subtract the value instead of adding",
          "default": false
        },
        "function": {
          "type": "string",
          "enum": ["statToModifier"],
          "description": "Named function (for numberFunction)"
        },
        "event": {
          "type": "string",
          "description": "Event name (for emitEvent)"
        },
        "rule": {
          "$ref": "#/definitions/rule",
          "description": "Rule to generate or offer"
        },
        "legalWhen": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/illegalWhenEntry"
          },
          "description": "Conditions for rule legality (for offerRule)"
        }
      },
      "additionalProperties": false
    },
    "illegalWhenEntry": {
      "type": "object",
      "required": ["condition"],
      "properties": {
        "condition": {
          "$ref": "#/definitions/condition"
        },
        "illegalDiagnostics": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/diagnostic"
          }
        }
      }
    },
    "diagnostic": {
      "type": "object",
      "required": ["code", "severity"],
      "properties": {
        "code": {
          "type": "string"
        },
        "severity": {
          "type": "string",
          "enum": ["error", "warning", "notice"]
        }
      }
    }
  }
}
